// configure development API
var api_config = {
	content_service_uri : "api/data/",
    layout_service : "api/workspaces/",
    authentication_service_uri : "api/persona/"
};

// app starting theme
var theme_config = {
    ui: {
        start: "dark",
        opposite: "light"
    }
};